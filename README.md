# svc-pipe

Lightweight request pipe for serverless environments.

## Environment

- `SVC_DST` — destination service URL (required), e.g. `https://sub.example.com`
- `SVC_PFX` — optional path prefix filter, e.g. `/vl8989`

Both values must be taken from the origin server.
